# D-review: Code Unloader 1.4.6 — Three Bugfixes (v2)
**Reviewed:** 2026-04-29 (re-review of v2) · **Spec:** `docs/superpowers/specs/2026-04-29-cu-1.4.6-bugfixes-design.md` · **Verdict:** ready-to-plan

## 1. Context Scanned (delta from v1 review)

Re-read the spec only. Codebase context from the v1 review still applies and was not re-verified — none of the v2 changes touch new code surfaces (no new files, no new methods beyond what v1 already pinned to verified sources).

## 2. Findings by Severity

### Critical
None.

### Major
All three v1 Major findings are resolved cleanly:

- **Autoptimize / HTML-buffer-optimizer claim** — Resolved. Goal at line 134 now scopes Bug 3 to `script_loader_src`-hooked optimizers (Perfmatters, WP Rocket page-cache mode, hash-versioning, CDN URL-rewriters). Line 136 adds an explicit "NOT covered" line above the fold so a reader can't miss it. "Why filters" at line 265 corrects the overclaim. New "Out of scope" bullet at line 271 names Autoptimize, WP-Optimize combine mode, and FlyingPress static-bundling explicitly, with a one-line forward-looking note about how a future fix would look (output-buffer scan analogous to the `wp_head` inline-block scan). Expectations are now calibrated.

- **FrontendPanel.php:247 stale comment** — Resolved, and resolved better than the original review suggested. Versioning table row at line 289 consolidates the rendered HTML comment to a single `CDUNLOADER_VERSION` echo, dropping the `panel.js v9 | panel.css v9` literals entirely. This eliminates the drift class permanently rather than just patching one instance — a nicer fix than "bump the literals to v11".

- **Search-input placeholder** — Resolved. New "Update the search-input placeholder (PHP)" sub-section at lines 219–228 includes an explicit diff for `FrontendPanel.php:273`. Files section at line 275 calls out the placeholder edit alongside the filter-and-emit work. The "buried easter egg" rationale makes the connection between the JS change and the placeholder explicit.

### Minor
All six v1 Minor findings are resolved:

- **Stable ordering on snapshot merge** — Resolved. Files section line 62 specifies `INNER JOIN cu_group_items × cu_groups ON g.id = gi.group_id AND g.enabled = 1, ORDER BY gi.group_id ASC, gi.id ASC`. Edge-case bullet at line 58 explains the policy in user terms ("lowest-numbered (oldest) group wins").
- **`from_snapshot: true` flag** — Dropped. Line 40 explicitly documents the YAGNI decision and pins a re-introduction trigger (alongside its consumer).
- **Device/condition snapshot filtering** — Resolved. New "Out of scope" bullet at line 52 documents the consistency-with-`rule_map` rationale and the cross-cutting requirement if a future release wants to change this.
- **`PHP_INT_MAX` consistency** — Resolved. Line 169 swaps the literal `9999` for `PHP_INT_MAX` and explains the symmetry rationale. Risk assessment at line 321 reflects the same constant.
- **"Files" leftover phrasing for Bug 3** — Resolved. Line 275 reads cleanly: "register filters at `PHP_INT_MAX`, store rewritten URLs in a static map, emit the inline-script patch at `wp_footer` priority `PHP_INT_MAX` via `emit_rewritten_urls()`, AND update the search-input placeholder at line 273."
- **Test 2 ambiguity** — Resolved. Line 306 pins tab B to "AI Assets Scanner Step 4 (post-scan, 'Push to Code Unloader' button visible)" and clarifies the timing trigger ("Within 1s of the success notice in tab B").

### Nits
None worth surfacing. The two v1 nits (12 vs 10 lines for the storage fallback; v-comment cited correctly) were cosmetic and remain inconsequential.

## 3. Findings by Dimension
None outstanding. All v1 findings are closed; no new findings introduced by the v2 edits.

## 4. Verdict
**ready-to-plan**

The v2 spec resolves every Major and Minor finding from v1, in some places more elegantly than originally suggested (the FrontendPanel.php:247 consolidation is a structural fix rather than a literal patch). The Bug 3 timing approach (late inline-script patch via `wp_footer:PHP_INT_MAX`) and the comprehensive search filter (handle / src / rewritten_src / source_label / extracted filenames) are both intact and correct. Out-of-scope sections set honest expectations for HTML-buffer optimizers, device/condition snapshots, and `script_loader_tag`-only rewrites. Acceptance criteria in the Testing table are observable and pinned to specific UI elements.

Safe to hand to `writing-plans`.
